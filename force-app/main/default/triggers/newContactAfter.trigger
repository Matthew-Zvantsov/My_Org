trigger newContactAfter on CONTACT (after insert) 
{
    /* Contact newcon  = Trigger.new[0];
    System.debug('newcon.Id' + newcon.Id);
    System.debug('con.LastName' + newcon.LastName);
    Case newcase = new Case();
    newcase.ContactId = newcon.Id;
    newcase.Description = newcon.Message__c;
    insert newcase; */
}